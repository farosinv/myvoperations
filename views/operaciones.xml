<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2017 MyV Operaciones Logisticas & Faros Inversiones Ltda.
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->

<odoo>
<data>

    <record model="ir.ui.view" id="operaciones_form_view">
        <field name="name">operaciones.form (in myv_port_operations)</field>
        <field name="model">operaciones</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <!-- TODO -->
                </header>
                <sheet>
                    <!-- TODO -->
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
                <div class="oe_chatter"></div>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="operaciones_search_view">
        <field name="name">operaciones.search (in myv_port_operations)</field>
        <field name="model">operaciones</field>
        <field name="arch" type="xml">
            <search>
                <!-- TODO -->
            </search>
        </field>
    </record>

    <record model="ir.ui.view" id="operaciones_tree_view">
        <field name="name">operaciones.tree (in myv_port_operations)</field>
        <field name="model">operaciones</field>
        <field name="arch" type="xml">
            <tree>
                <!-- TODO -->
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="operaciones_act_window">
        <field name="name">Operaciones</field> <!-- TODO -->
        <field name="res_model">operaciones</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record model="ir.ui.menu" id="operaciones_menu">
        <field name="name">Operaciones</field>
        <field name="parent_id" ref="xmlid of parent menu"/> <!-- TODO -->
        <field name="action" ref="operaciones_act_window"/>
        <field name="sequence" eval="16"/> <!-- TODO -->
    </record>

</data>
</odoo>
